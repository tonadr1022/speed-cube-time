export const minCharMessage = (length: number) => {
  return `Must be ${length} or more characters long`;
};
export const maxCharMessage = (length: number) => {
  return `$Must be ${length} or less characters long`;
};
